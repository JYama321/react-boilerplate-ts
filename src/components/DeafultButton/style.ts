import styled from "styled-components";

export const DefaultButton = styled.button`
  display: inline-block;
  width: 120px;
  height: 40px;
  background-color: rgba(30, 30, 250, 0.7);
  outline: none;
  border: 1px solid rgba(0, 0, 0);
  &:hover {
    cursor: pointer;
  }
`;
